apiveersion: app/v1
kind: ReplicaSet
metadata:
 name: myapp-replicaset
 lables:
  app: myapp
 specs :
   selectors:
     matcLables:
      app: myapp
   replicas: 3
   templates:
     metadata:
       name: nginx-2
       lables: 
         app: myapp
    spec:
      container: 
        - name: nignx
          image: nginx